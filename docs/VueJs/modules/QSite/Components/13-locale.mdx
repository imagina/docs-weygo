import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# locale
Show images as a preview and full image in a modal.
<Tabs>
  <TabItem value="Props" label="Props" default>
    <table>
      <tr>
        <td>value</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Object</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Bidirectional configuration for the component.</p>
        </td>
      </tr>
      <tr>
        <td>form</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>DOM Object</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p><a href="https://quasar.dev/vue-components/form">q-form</a> element.</p>
        </td>
      </tr>
    </table>
  </TabItem>
  <TabItem  value="Value_Configuration" label="Value Configuration" default>
    <table>
      <tr>
        <td>fields</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Object</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Define non-translatable fields with default data.</p>
        </td>
      </tr>
      <tr>
        <td>fieldsTranslatable</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Object</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Define translatable fields with default data.</p>
        </td>
      </tr>
      <tr>
        <td>form</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Object</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Form data with Laravel translations structure.</p>
        </td>
      </tr>
      <tr>
        <td>formTemplate</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Object</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Contains form data by the current language.</p>
        </td>
      </tr>
    </table>
  </TabItem>
</Tabs>

## Usage

1. Import the `locale` component and define it within your component:

    ```js
    import locale from '@imagina/qsite/_components/master/locale'
    ```

2. Use the `locale` component within a `q-form` element. Make sure to set a reference to the form element using the `ref` attribute:

    ```jsx
    <q-form ref="formContent">
      <!--Locale-->
      <locales v-model="locale" ref="localeComponent" :form="$refs.formContent"/>
    </q-form>
    ```

3. Configure the `locale` object to define the form structure:

    ```javascript
    // Locale configuration
    locale = {
      form: {},
      fields: {
        userId: this.$store.state.quserAuth.userId,
        countryId: null,
        provinceId: null,
        cityId: null,
        status: '2',
        checked: null,
        featured: null,
        lat: null,
        lng: null,
        map: null
      },
      fieldsTranslatable: {
        title: null,
        slug: null,
        description: null,
      }
    };
    ```
