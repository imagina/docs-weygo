import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# uploader
Pick a file using a selector to upload and use the cropper component to edit image files.

<Tabs>
  <TabItem value="Props" label="Props" default>
    <table>
      <tr>
        <td>accept</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>String</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>String defining the accepted formats, separated by commas.</p>
        </td>
      </tr>
      <tr>
        <td>maxFiles</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Number</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Number of files to upload.</p>
        </td>
      </tr>
      <tr>
        <td>emitBase64</td>
        <td>
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Define if files should be emitted in base64 format.</p>
        </td>
      </tr>
      <tr>
        <td>emitFile</td>
        <td>
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Define if files should be emitted as File objects.</p>
        </td>
      </tr>
    </table>
  </TabItem>
</Tabs>

## Usage

1. Include the `uploader` component in your template:

   ```jsx
    import uploader from '@imagina/qsite/_components/master/uploader
    <!--Uploader-->
    <uploader v-model="responseValue" :max-files="3"/>
   ```
