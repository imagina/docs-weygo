import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# btnMenu
Use [q-menu](https://quasar.dev/vue-components/menu) quasar component to load a button with a actions list.
<Tabs>
  <TabItem value="Props" label="Props" default>
    <table>
      <tr>
        <td>actions</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Array</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Action list.</p>
          <i class="colorGraySpan">Example:</i>
          <p><code>[label, icon, vIf, action, route]</code></p>
        </td>
      </tr>
      <tr>
        <td>actionData</td>
        <td>
          <i class="colorGraySpan">Type:</i>
          <br />
          <code>Any</code>
          <br />
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Data to set as parameter on callback action</p>
        </td>
      </tr>
  </table>
  </TabItem>
</Tabs>

## Usage

The `btn-menu` component is already included in [boot-core-components](/docs/VueJs/modules/QSite/QsiteBoot#core-components), so there's no need to import it separately.

```jsx
  <btn-menu :actions="actions" :action-data="actionData" />

  actions: [
    {
      icon: 'fas fa-pen',
      color: 'green',      
      label: 'Action label',
      vIf: true,
      action: (data) => {
        // Do anything...
      },
    },
  ],

actionData = 5 + 3,
```
