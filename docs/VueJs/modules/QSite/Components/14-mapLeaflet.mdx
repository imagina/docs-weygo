import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# mapLeaflet
Implement [`vue2-leaflet`](https://vue2-leaflet.netlify.app/) component.
<Tabs>
  <TabItem value="Props" label="Props" default>
    <table>
      <tr>
        <td>height</td>
        <td>
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>CSS height to render the map.</p>
        </td>
      </tr>
      <tr>
        <td>value</td>
        <td>
          <i class="colorGraySpan">Description:</i>
          <br />
          <p>Value with latitude and longitude.</p>
        </td>
      </tr>
    </table>
  </TabItem>
</Tabs>

## Usage

1. Import and define the `mapLeaflet` component in your component:

    ```js
    import mapLeaflet from '@imagina/qsite/_components/master/mapLeaflet'
    ```
2. Use the `mapLeaflet` component:
    ```jsx
    <map-leaflet v-model="responseValue" />
    ```